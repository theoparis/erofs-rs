[package]
name = "fuse"
version = "0.1.0"
edition = "2024"

[dependencies]
fuser = { version = "0.17", default-features = false }
erofs-sys = { path = "../erofs-sys" }
clap = { version = "4", features = ["derive", "cargo"] }

[features]
default = []
libfuse = ["fuser/libfuse"]
